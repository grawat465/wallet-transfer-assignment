spring:
  application:
    name: wallet-transfer
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/wallet_transfer}
    username: ${DB_USERNAME:wallet}
    password: ${DB_PASSWORD:wallet}
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: ${SERVER_PORT:8080}
